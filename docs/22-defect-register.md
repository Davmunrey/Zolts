# 22 · Defect register

Every defect this repository has found in itself, what it would have cost, and what now stops it returning.

**None of these reached a customer.** There is no production deployment and no customer data (`docs/20`). They are development defects, and the register exists because *how* they were found is the more useful artefact than the list itself.

## The finding method, and its yield

| How | Defects found | What it means |
|---|---|---|
| **Executed** | 26 | Ran the thing against real infrastructure — real Postgres, real browser, real container |
| **Read** | 40 | Compared a document against the code, looking for the caller that does not exist |
| **Mutated** | 7 | Broke a guard on purpose to see whether it bites |
| **Looked** | 5 | Rendered a screen and read the screenshot |
| **CI** | 4 | An existing check fired |

**This table used to say 16 executed and 3 read.** Counting the rows gives 14 and 8. The register overstated its own method by five, in the direction that flattered it, and nothing measured it until a test did — the same defect as everything in it, committed by the document that catalogues them. `tests/test_registers.py` now counts the column, so the numbers above are a measurement rather than a memory.

**Mutation is now the only method that runs on every push.** `scripts/mutation_check.py` breaks sixteen named guards and requires a test to notice; a mutation whose target has moved is an error rather than a skip, because code moving out from under a check is exactly when the check stops being applied. It is not a coverage measurement and `docs/24` VER-1 keeps that item open — it re-proves a curated list, and says nothing about the code it does not name.

**And now a measured number rather than a list.** `scripts/mutation_coverage.py` samples the mutant space — comparison and boolean operators, integer constants — applies each one as a source edit and runs the suite that could catch it. On `zolts/`, against the database-free suite: **39 of 40 caught, 97.5%, 95% CI 87-100%, sampled from 512 possible mutants**. The one survivor was `zolts/programtest.py:130`, the hour of a fixed clock the policy engine never reads — an equivalent mutant, now annotated as one rather than papered over with a test that would assert a no-op. Equivalent mutants count as survivors here, which pushes the measured number below the truth rather than above it.

The runtime is a separate measurement and a slower one: its behaviour is only reachable with Postgres, so each mutant costs a full suite run. A target whose suite the script did not run is a target it refuses to report on — a coverage number produced by skipping the tests that would catch it is worse than no number.

**And it is a different number.** A full sample over `runtime/` — thirty mutants, each
against the whole Postgres-backed suite — measures **22 of 30 caught, 73%, 95% CI 56-86%,
sampled from 1,467 possible mutants**, against `zolts/`'s 97.5%. The gap is real and is
what one would expect: the pure core is reached by every test, and the runtime's behaviour
needs a database, a tenant and a programme before most of it runs at all. Measured at
`4361628`. The guards written since kill four of the eight below — `e4f05da` the heartbeat,
`b8f5cf9` the command line's `authenticated` flag, and this change the lift's precision and
the baseline date's ceiling — so the same sample at the current head is **26 of 30**, with
only the four equivalent mutants left alive.

**That 26 is not a coverage rate, and must never be quoted as one.** It is the same thirty
mutants, re-scored after writing tests against the specific eight that survived them. A
sample you have fitted to measures the fitting, not the suite: the honest number for the
runtime is still 73% with a 56-86% interval, and the only thing that can replace it is a
**fresh sample at a new seed**. What 26 of 30 does say — the whole of it — is that every
survivor from that run was either killed or shown to be equivalent, which is the audit
trail, not the metric.

**A fresh sample, and it is the number that replaces the old one.** Seed `20260908`,
thirty mutants, a new draw over `runtime/` at `831d614`: **27 of 30 caught — 90%, 95% CI
74-97%, sampled from 1,473 possible mutants.** This one is not fitted to. It was drawn after
the guards were written, against mutants nobody had seen, which is the only condition under
which a rate means anything.

**And the improvement is not statistically significant, which has to be said in the same
breath.** 22 of 30 against 27 of 30 is a two-proportion `z` of 1.67 and a two-tailed *p* of
0.095. At thirty mutants a sample cannot resolve seventeen points. What can honestly be
claimed is narrower: the point estimate moved from 73% to 90% while the suite grew from
1,181 tests to 1,235, the intervals overlap, and a third sample would be needed to call it.
The temptation here is to report 90% and stop; the reason not to is that the same arithmetic
would have made 73% look like a crisis, and it was not one either.

Three survived. `console.py:663` was real and the most useful thing the run produced:
`content.get("body") or ""` mutated to `and ""` returns the empty string whenever a body is
present — so every draft in the review queue renders blank. `review_view` had no test at
all, which is why nothing noticed. A queue that shows a reviewer nothing and asks them to
approve it does not fail; it collects approvals that are worth nothing, against invariant 1.
`cli.py:584` returned 2 for a tenant that does not exist, an exit code three other tests
already treat as *the input was wrong*, and this path had none. Both are guarded.
`onboarding.py:137` — six hex characters of random slug suffix against eight — is equivalent
on a column with no length ceiling, and is annotated where it lives.

**Half the survivors were equivalent.** Four of the eight change nothing any test can
reach — a strict float comparison whose equality case cannot be constructed, a
connection-pool cap, the last step of a retry backoff moved by one second, and the number
of characters a fallback identifier is truncated to. They are annotated where they live
rather than answered with a test that asserts a no-op.

**The other four were real, and all four are guarded.** `worker.py:413`: the heartbeat
detail is everything the tick carries except its errors, and `docs/25` sends an operator to
that row to read `claimed`. `cli.py:704`: the command line printed the inverse of whether a
data provider holds a credential, and the failure ran the expensive way — a provider
reported as authenticated is one nobody goes and connects, and every lookup through it
errors until somebody notices. Both sides of that one are asserted, because a one-sided
check is exactly what let D-57 live.

The last two were carried for one commit as declared debt, on the judgement that a decimal
and an unreachable length ceiling were not worth the fixtures. That judgement was wrong,
and cheaply so: both took one fixture each. `console.py:110` rounds a frozen report's lift
to three decimals where the rest of the panel is in two. `schemas.py:136` loosens a
baseline date's length ceiling from ten characters to eleven — the window the partner signs
a letter against. Both are killed now, and re-applying each mutation fails exactly its own
test and no other.

**The lift figure is D-58, and it is the second shape again.** A test already asserted that
figure. It asserted it as `shown["liftPp"] == round(frozen[...]["lift"] * 100, 2)` — the
code's own expression, re-run — on a fixture whose lift was exactly 15.00pp, where two
decimals and three agree. Self-referential *and* fed data that could not discriminate: the
assertion would have survived any rounding change at all. The replacement expects a
literal, on a lift of 23.333…pp, and asserts first that the fixture still discriminates, so
that a later change to the counts fails loudly instead of going quiet.

**That an equivalence rate of half is the interesting number.** A survivor list where
every entry is real would mean the generator is too timid to reach the boundaries that
matter; one where nearly all are equivalent would mean the measurement is theatre. Four in
eight, with the real ones landing on a runbook's own instructions and a connector's
pagination, is a sampler working as intended.

An earlier partial pass over the same seed, before the guards of the previous change, left
nine alive in nineteen. The same seed produces the same mutant sequence, so that is a
controlled before-and-after: four mutants that survived then are caught now.

The worst of the seven: Pipedrive's pagination stopped after the first page, Salesforce contacts lost the mailing country the policy engine resolves jurisdiction from, and the planner counted every email open as zero — so a play branching on *opened but did not reply* would have stopped firing, silently, for everyone. None of the three is a wrong line anybody would spot by reading; each is a line no test distinguished from a different line.

**What survives the correction is the sharper claim.** All twenty-two of the read defects were found by reading a *document* against the code — a price nothing charges, an ADR written in the present tense about a connector that does not exist, a script nobody had ever run. Not one was found by reviewing code for a wrong line. Reading works, and what it finds is the absent caller, never the incorrect one.

## The recurring shape

Forty-nine of the eighty-two are one defect wearing different clothes: **a complete specification with no caller.** A price in `docs/12` nothing charges. A column no code sets. A button labelled for a feature that does not exist. A process table CI never runs. A restore script that ends by telling you to test it.

The second shape, found only once the first was exhausted: **a guard that passes when you break the thing it guards.** Nine of those, and two of them were written in the same session that found them.

The third is the second one's cheaper cousin and appears last, because you only see it after you stop asking whether a guard is correct and start asking who calls it: **a guard that is correct, tested, and not on the path.** D-34 is that — four checks, all of them right, installed in one of five callers, and the caller they were missing from was the one no operator watches.

**The fourth shape is the first one this register found by breaking something, and it is the one reading could never have found: a line that is correct on every case anyone wrote down, and undefined on the one nobody did.** D-55 is that. `names()` reports a path and its own prefixes; both bind the same name; the winner was whichever the `set` iterated last. Every existing test passed, the code reads correctly, and the behaviour depended on the process hash seed — so the defect was not that a clause failed, but that a clause failed on *some workers*, after a restart, with no deploy in between.

It arrived the way it had to. The sampler in `scripts/mutation_coverage.py` left a mutant alive in the depth check — `>` for `>=` — which is a boundary nothing asserted from both sides. Writing the test that kills that mutant meant evaluating a path at exactly the limit, which is three segments, which is the first depth at which a path has a prefix. The mutant was equivalent in the end. The test written to kill it was not. **That is the argument for measuring coverage rather than curating it: the yield is not the mutant, it is the test you are forced to write.**


**The first shape stopped needing a reader.** Thirty-seven instances found one at a time, by
holding a document against the code and looking for the caller that is not there — a method
that works and does not scale, because it depends on somebody deciding to look. Three more
arrived at once from a script instead.

`scripts/unused_indexes.py` resets the statistics, runs the whole suite as a workload, and
reads `pg_stat_user_indexes.idx_scan` back. An index is chosen by the planner and never named
in a query, so "does anything use this" is not a question grep can settle — but Postgres has
been counting the answer all along. Ninety-nine indexes, seventy-seven scanned, eighteen
backing a key or a unique constraint and earning their cost on write. Four plain indexes were
never touched. Three of them were D-60 and D-61.

Two things make the result trustworthy rather than merely alarming. The first is that a zero
here means *no test needed it*, not *production would not use it*: test tables hold tens of
rows and the planner rightly prefers a sequential scan on those, so the measurement
over-reports and is a question to answer rather than a verdict to act on. Every one of the
four was read by hand before anything was changed, and the fourth — `outcome_unverified` —
turned out to be a correct index that the suite is simply too small to exercise, and was left
alone.

That fourth one is also what keeps the tool from becoming decoration. Every accepted zero
carries a written reason in the script's own `ACCEPTED` list — what settles the question
instead of a scan count — and a test asserts each named index still exists, because an entry
for an index that has since been dropped reads as diligence while suppressing nothing, and the
next real finding would hide behind it. The three indexes migration 025 adds are accepted on
the strength of a stronger claim than a counter: `EXPLAIN` plans a query without running it,
so it moves no statistic, and the plan is what the tests read.

The second is the shape the survivors had in common, which no amount of reading had made
visible: **a partial index whose predicate is the complement of the only query that exists.**
`where phone is not null`, against a sweep that asks for the people who have no phone. Written
from the noun instead of from the access pattern. It cannot be caught by reading the index,
because the index is well-formed; it can only be caught by reading the index *and* the query
and noticing they are about disjoint sets of rows.

**The fifth shape is the only one that costs nothing when it fires and everything when it is
tolerated: a check that fails on its environment rather than on its subject.** Six of them,
and the third is what named the class.

* **D-35.** `smoke_runtime.py` failed about one run in ten because the account it enrolled was
  assigned to the holdout by a hash of its id. Correct product behaviour, reported as a broken
  smoke.
* **D-59.** Two preflight tests asserted the no-TLS branches of a check while inheriting
  whatever the ambient Postgres did. Green on CI's `postgres:16`, red on a Debian package that
  ships `ssl = on`, against a product correct on both.
* **D-68.** The demo seeds the example programme, whose send window closes at 18:00
  Europe/Madrid. Outside it the planner correctly defers every touch to the next morning, the
  seeder reports nothing sent, and the suite fails. Fifty green hours in a hundred and
  sixty-eight — red about seventy per cent of the week, for months, on code that was never
  wrong.
* **D-71.** The verification gate itself. Sixteen of its eighteen guards are held by tests
  that need a database, and a skipped test exits zero exactly like a passing one — so on a
  machine without one the script broke eight guards, watched the rest of each file pass, and
  called them survivors. Ten of eighteen, in red, about code that was never wrong.
* **D-73.** Twenty-four tests assert what the restricted database role cannot do. The fixtures
  fall back to the owner when no separate role is configured, and the owner bypasses every
  grant, so a contributor who exports the one obvious URL gets twenty-four failures — eighteen
  of them tenant scoping. The first count was nine, which is what a hand probe over three
  files could see; `scripts/ambient_check.py` found the other fifteen in one run.
* **D-74.** A fleet test seeded a mailbox at `date.today() - 13 days` and asserted the runtime
  read it as warm-up day fourteen. The runtime counts from the UTC date, deliberately, because
  a mailbox is the same age everywhere; `date.today()` is the machine's local date. East of UTC
  after local midnight the two differ by a day. Red for twelve hours a day in New Zealand and
  never once in Europe. **Found by `scripts/ambient_check.py` on its first full run** — the
  script written for exactly this, one merge after the register said the probe was ad hoc.

None of the six was a product defect. The first five were found the same way: something went red
that had every hallmark of a flake — an unrelated file, passing an hour earlier, passing in CI
on the same commit — and the rule that *"flake" is not a root cause* was followed anyway. D-68
surfaced twenty-two minutes after a window closed, on a run CI had passed at 16:02. D-71 was
found by running the gate twice, once with a database and once without, and reading the
difference rather than the second number, and D-73 the same way one command later.

**Five ad-hoc probes is a method nobody can repeat**, which is where hand-verified guards stood
before `mutation_check.py`. `scripts/ambient_check.py` runs the suite under named ambient
conditions and reports the tests whose *verdict* moves; a test that skips because the condition
honestly removed its premise is the correct answer and is reported separately. Its first run
found the fifteen D-73 entries the hand probe had missed.

The cost is not the failure. It is that a suite which is red on correct code teaches the team
to press re-run, and a team that presses re-run has no suite at all. **A test may not depend on
ambient time, ambient configuration or ambient data.** Where it must touch one, it establishes
the premise itself and asserts it first — `sslmode=disable` and a check that the connection
really is plaintext; a programme with the window removed and a check that the shipped one still
declares it. That assertion is the difference between a test and a coincidence.


## Register

Severity is the cost of the defect reaching a paying customer, not the cost of fixing it.

### Money

| ID | Defect | Blast radius | Found | Fix | Guard |
|---|---|---|---|---|---|
| D-03 | `cost_event.billed_credits` existed from the first migration; no caller ever set it. `docs/12` priced eight actions; the runtime billed nothing | **Critical** — the entire product was free | Read | `zolts/billing.py` holds the list; metering writes it | A test parses `docs/12` and asserts the code agrees (ADR-017) |
| D-04 | A credit was recorded only `if result.cost_micros`, so a send through a provider that reports no COGS was free to the customer | **Critical** — revenue silently lost per provider | Executed | Billing does not depend on knowing our own cost | ADR-017 |
| D-05 | The overage ladder and the extra seat were priced in `docs/12` and charged by nothing; the runtime reported the rate as "contractual" | **Critical** — expansion revenue, the first NRR driver, was arithmetic nobody could run | Read | Both charged, graduated over the overage alone | A test re-reads each number out of `docs/12`; a property test asserts the bill never falls as consumption rises |
| D-06 | Every tenant stopped at their included credits — a ceiling that cannot be raised, so overage was unreachable in the product | **High** — no tenant could overspend, so none could expand | Executed | `tenant.credit_ceiling`, defaulting to the included allowance | ADR-017 |
| D-08 | Every dispatch was priced at `email.send`. `task` and `crm` both have providers, so a HubSpot task written for a salesperson was billed to the customer as **an email they never sent** | **Critical** — an invoice a customer can disprove | Executed | `runtime/channels.py`: a channel carries its own credit kind | ADR-027; four call-site mutations, all caught |
| D-11 | `program.step`, the highest-volume priced action, was billed by nothing | **Critical** — the volume line of the price list was free | Read | `metering.meter_step`, charged when the runtime ran it | ADR-024 |
| D-12 | `agent.dossier` was priced at 20 credits and was neither executable nor stored | **High** — a priced artefact that did not exist | Read | `runtime/research.py`, stored and reused | ADR-025 |
| D-30 | The application role lacked `usage` on a `bigserial` sequence, so an insert was denied | **High** — a feature that worked in development and failed for every tenant | Executed | `grant_app_role` grants on all sequences | A test asserts every sequence is usable by the app role |

### Correctness and measurement

| ID | Defect | Blast radius | Found | Fix | Guard |
|---|---|---|---|---|---|
| D-01 | The internal schema was named `zolts` beside a role named `zolts`. Postgres resolves `"$user"` before `public`, so a second migration run created a complete duplicate of every table in the shadowing schema — silently, reporting success | **Critical** — queries returning the wrong data with no error | Executed | Schema is `zolts_internal`; every connection pins `search_path` | ADR-009, plus a test that the schema is not named after a plausible role |
| D-02 | Every reply was recorded as `reply_positive`. "Take me off your list" was counted as a conversion and the person left contactable | **Critical** — a compliance failure and an unbounded inflation of the only number the product sells | Executed | Triage classifies; the runtime disposes; a verdict must quote the reply | ADR-016; the unread share is reported beside the lift |
| D-20 | `programs.publish` was a plain upsert. Republishing v2.1.0 with different content replaced the spec of a version already live, under every enrollment running on it | **Critical** — the audit log says one thing, the program says another, and one measured lift covers two programs | Mutated | A version is written once; identical content still succeeds | ADR-030; 409 on conflict |
| D-22 | `policy_decision.rationale` was nullable and `record_decision` took `str \| None`. The gate could record a deny with no reason and the entire suite passed | **High** — the console renders a decision nobody can explain, on the screen a regulated buyer opens first | Mutated | Migration 018: `not null` plus a non-blank check; `DecisionWithoutAReason` | ADR-031 |
| D-29 | `now()` is the transaction timestamp, so two dossiers written in one transaction shared it exactly and "the latest" was undecidable | **Medium** — a stale dossier served as current | Executed | A `bigserial` decides order | A test asserts one distinct `created_at` while `latest()` still returns the second |
| D-07 | The sync never read the connection's `config` column, so a CRM with a per-org host was unreachable | **Medium** — Salesforce could not be connected | Executed | `from_config` on the source contract | ADR-018 |
| D-10 | `linkedin`, `ads` and `webhook` were dispatchable with no provider. The shipped flagship program's four LinkedIn steps queued, failed as permanent errors and were cancelled one at a time | **High** — loud in a row nobody reads is silent | Executed | An unexecutable channel becomes a person's task | ADR-027 |
| D-09 | Every dispatch allocated a mailbox seat, so a CRM task consuming no sending reputation was held by the mailbox daily cap | **Medium** — work stalled by a limit from a channel it is not on | Executed | Only channels that use the fleet allocate | ADR-027 |
| D-28 | `console.build` was handed a label dictionary instead of the tenant's row; the spend view reads a billing period keyed by id | **Low** — a smoke script passed, CI failed | CI | `build` raises a named error when the tenant has no id | A test holds the stub deliberately |
| D-46 | `docs/10` said every program reports a real income statement — cost by kind, incremental pipeline, ROI and cost per incremental meeting, *derived* — and called that view the product's primary screen. Nothing derived it. The measurement endpoint returned rates and the console returned spend and, when significant, a pipeline figure from a constant; no artefact joined cost to lift, and every figure was recomputed on each request. The baseline was frozen (ADR-042) and the "after" it exists to be compared with moved every day | **High** — the Act 2 conversation (`docs/17`) had a signed "before" and nothing a CFO could hold beside it; a pilot's final read was whatever the endpoint said on the day somebody looked | Read | `zolts/report.py` composes the report from what the runtime records, `runtime/reporting.py` freezes one per program at every period close, written once, with the document stored verbatim and a digest over inputs and derived figures (ADR-043) | `test_incrementality_report.py`; the serving role cannot update or delete a report; `an-unresolvable-comparison-has-no-verdict` in `scripts/mutation_check.py` |
| D-54 | `scripts/mutation_coverage.py` writes a mutation into a source file, runs the tests and restores it in a `finally`. A `finally` covers an exception and not a signal: stopping a long run with SIGTERM left `runtime/cli.py` carrying `return 1` where it had `return 0`, in this repository's own working tree, and the next commit would have carried it | **Medium** — a tool that measures the tests silently editing the code the tests measure; found by interrupting a run of it | Executed | The mutation is applied inside a context manager that restores the file on SIGINT and SIGTERM before re-raising, and the script still refuses a dirty tree | `test_mutation_coverage.py::test_an_interrupted_run_puts_the_file_back` drives it in a subprocess against a temporary file and kills it mid-mutation |
| D-53 | `zolts/policy.py` opened with *packs are data, not code, so a regulatory change ships without a deployment*, and the only pack was a dict in that file — a change needed a release. Underneath it, `policy_decision` recorded a rule key, a jurisdiction and a reason while the rules behind them moved with every deploy, so the question the table exists to answer — *under which rule was this person contacted* — resolved to whatever the code said today | **High** — the audit trail a DPO and a diligence reviewer read, pointing at a moving target, and the product invariant that every action records a decision *with a reason* half-kept | Read | A pack is a published document (`runtime/policy_packs.py`, ADR-044): one active at a time, published by an operator rather than a tenant, installed with the schema, and every decision carries the version and digest of the pack that produced it. `record_decision` refuses a decision that cannot name its pack | `test_policy_packs.py`; `the-gate-decides-under-the-published-pack` in `scripts/mutation_check.py`; the Policy view reports decisions that name no pack rather than hiding them |
| D-52 | The pilot letter in `docs/26` reads each success criterion out of the frozen report by name, and the positive-reply-rate row pointed at `primary.treatment_rate`. Once a programme is measured on its own declared metric (D-51), that field is the rate of *opportunities* for the flagship programme — so the letter's first criterion, the one that resolves earliest in a pilot, named a number that no longer means what the row says. The "what runs" paragraph likewise still defined a conversion as a positive reply, a meeting or an opportunity | **Medium** — a signed criterion read off the wrong field, in the document a pilot is judged by | Read | The row reads `converted_by_type.reply_positive.treatment` over `primary.treatment_enrolled`, and the paragraph defines a conversion as what the programme's `primary_metric` counts | A test parses every backticked path out of the letter's criteria table and resolves it against a real canonical report; a criterion naming a field the report does not carry fails the build |
| D-51 | `examples/schema` requires `experiment.primary_metric`, the console printed it on the programme's own panel, and **nothing measured by it**. Every programme was counted the same way: any of `opp_created`, `meeting` or `reply_positive`, at any time after enrolment. So the PLG programme declaring `paid_conversion_30d` was judged on positive replies, the ecommerce one declaring `net_revenue_28d` likewise, and a ninety-day metric counted a conversion from month six — comparing a treatment arm enrolled in January against a control arm still accumulating in June | **High** — the verdict on every programme was about a different thing from the one it declares, and the frozen report carried that verdict to a partner | Read | `zolts/metrics.py` resolves a declared metric into the outcome types it counts and the window it counts them in; the measurement endpoint, the console and the report all read it, admission refuses a metric nothing can count, and a value metric's amount is reported and explicitly never tested | `test_metrics.py`; `a-conversion-outside-the-window-does-not-count` in `scripts/mutation_check.py` |
| D-49 | The console's incremental pipeline multiplied the lift in *conversions* — a set that is mostly positive replies — by `AVG_OPPORTUNITY_EUR`, a constant in `runtime/api/console.py`. It priced a reply as a deal, and it priced an ecommerce reorder and an enterprise contract at the same €24,000. `docs/10`'s formula is `absolute_lift × N_treatment × average_opportunity_value`, over a programme whose primary metric is opportunities | **High** — the number a buyer is shown first, overstated by the whole reply-to-opportunity ratio, and the one a CFO checks against their own CRM | Read | Pipeline rests on the opportunity comparison and on the average of the tenant's own synced deals, computed by the same `zolts.report` rules the frozen report uses; the runtime carries no deal size of its own, and the figure is withheld with a reason when either is missing | `test_console_views.py`, `test_fixture.py`; the frozen report and the live panel are the same object |
| D-50 | `scripts/seed_demo.py` — the sales demo — seeded accounts with `industry=`, and `entities.upsert_account` reads `industry_code`. Every demo account had none, the flagship programme's audience is `industry_code = 'software'`, and from the day the audience block began to execute the demo enrolled **zero** accounts. Two more in the same script: the audience also refuses to enrol until a CRM has delivered deals (ADR-038) and nothing ever synced one, and the last line handed `console.build` a dictionary of labels, which raises. Nothing in CI ran it | **High** — the artefact a buyer is walked through, producing an empty tenant, found by running it rather than by reading it | Executed | The population carries `industry_code` and is weighted towards the audience it is shown against, a demo CRM delivers the deals both before and after the run, and the seeder reads the tenant's own row | `test_demo_seed.py` runs the whole script at 400 accounts against a real Postgres and asserts enrolments, sends, a measurement and the deals left alone |
| D-47 | A provider document's `confidence` path was read as `min(1.0, float(raw))`. Providers do not agree on the scale: one returns `0.92` and another returns `92` for the same belief, and the second arrived as **1.0** — certainty. That number is what `enrichment.provenance()` shows an operator defending a record and what an accuracy floor is read against | **Medium** — a 76% guess presented as a fact, on the surface built to answer a DPO | Read | The document states its own scale in `confidence_max`, applied before the clamp; a scale with no confidence path to scale is refused | `test_provider_documents.py`; `a-percentage-score-is-not-certainty` in `scripts/mutation_check.py` |
| D-48 | `install_declared` registered a tenant's provider document under the document's own `metadata.provider`, and the waterfall asks `get_provider(row["key"])`. A row keyed `hunter-io` holding a document named `hunter` was accepted by the CLI, chosen by the optimiser, and then not found — and `LookupError` is not what the buy loop catches, so it left the enrichment step rather than skipping that provider | **Medium** — a registration the runtime accepts and cannot use, failing on the first real lookup rather than at registration | Read | One name: `provider_of()` is the single definition, the installer refuses the mismatch, and the CLI refuses it before the row exists | `test_provider_documents.py::test_a_document_and_its_registration_answer_to_one_name` |
| D-55 | `zolts/expr.py` binds each path `names()` reports into the evaluation scope. `names()` reports an attribute chain *and its own prefixes*, so `payload.a.a` also yields `payload.a`, and both bind the name `a`. They were bound in the order a `set` iterated, so the shallower one could land last and overwrite the namespace with its raw dict — and the next segment raised `AttributeError: 'dict' object has no attribute 'a'` | **High** — set iteration over strings follows the process hash seed, so this was not a clause that failed but a clause that failed *on some workers*: a depth-three `where` enrolled correctly, then stopped enrolling after a restart, with no deploy in between. The error also escaped the `TypeError` guard D-37 put around trigger evaluation, so it surfaced as a 500 on `POST /v1/signals` | Mutated | A dotted path always wins over a bare one at the same name, and `_Namespace` collects a whole branch before assigning any of it. The namespace is the only binding under which every reported path resolves | `test_expr.py::test_a_clause_evaluates_the_same_under_every_hash_seed` runs the same clause on the same payload under eight `PYTHONHASHSEED` values in subprocesses and fails unless all eight agree |
| D-56 | Three colour defects the brand work surfaced, all the same shape. `--live`, the live-latency semantic, was declared and rendered nowhere, while the runtime's own p95 wore `tone: "good"` — the verified-lift green — unconditionally, so a slow runtime and a fast one were the same colour and that colour already meant something else on the same screen. A running programme's status dot wore it too, inside the programmes table. And `--ink-secondary` and `--line` were referenced by three CSS rules and declared by none, so three declarations were dropped in silence: the evidence list lost its row separators and two labels fell back to inherited ink | **Medium** — a CFO reads colour before text, and the surface was telling them a latency figure was a verified lift. CSS fails quietly, so nothing rendered wrong enough to notice | Read | Latency takes its own semantic; lifecycle is drawn in ink because it is not a measurement; the two undeclared tokens are declared. ADR-045 makes the rule structural: inside a chart, table or figure, colour means a measurement and only a measurement | `test_brand.py` walks the stylesheet and fails if the accent paints a selector that renders a measured value, if a declared semantic has no caller, or if any `var()` names a token nothing declares |
| D-60 | Three enrichment indexes were built from the noun rather than from the access pattern. `person_phone_ix` covered `(tenant_id, phone) where phone is not null`, and the only query that exists is the enrichment sweep — `select * from person where phone is null order by created_at limit N` — which asks for exactly the rows the index excludes. `action_billed_ix` covered `where billed_at is not null` while the only statement reading `billed_at` finds its row by primary key and filters `billed_at is null`. The `email` and firmographics sweeps had no index at all | **Medium**, and invisible by construction: a partial index whose predicate is the complement of the query is never *wrong*, it is merely never chosen. Nothing fails and nothing is slow at test volume. The bill is paid on every write to `person` and to `action` — the highest-volume table in the schema, one row per programme step per enrolment — for a read that cannot happen. Email is the field the waterfall buys most and the one DATA-1 seeks a second supplier for, and it had no index at all | Executed | Migration 025 indexes the query that exists, ordering included: `(tenant_id, created_at) where <field> is null`, so the planner walks the index and stops at the limit instead of scanning and sorting. The two that could never be read are dropped | `test_index_usage.py` reads the plan with sequential scans disabled and asserts both that the intended index is chosen **and** that no `Sort` node appears. Verified by restoring the inverted index (both assertions fail) and by building the right rows without the ordering column (only the `Sort` assertion fails) |
| D-61 | `inbound_event_unhandled_ix` was declared in migration 005 and no query ever asked the question it answers. An inbound event is handled in the request that receives it; when that handler raises, the row keeps `handled = false` and its error, and nothing looks at it again | **High**, and silent in the most expensive way. The webhook was accepted, so the provider is satisfied and will not retry. Suppression and measurement are both fed from these events, so an unhandled row is potentially a suppression never applied — a contact who asked to stop and will be written to again — and a conversion never counted, in the lift a partner is invoiced against. `docs/25` covered outbox health and said nothing about inbound | Executed | A seventh liveness signal, `inbound handled`, with a threshold and an age floor so an event still on the stack is not counted. `docs/25` gains the procedure, which puts unsubscribes before everything else | `test_index_usage.py` asserts both sides and the in-flight case; the runbook's own output is the command's |
| D-64 | Four shipped blueprints declare governance controls that nothing reads, and they are the regulated archetypes. `healthtech-lifesci` states `special_category_inference: forbidden` and `copy_approval_required: true`; `fintech-regulated` and `public-sector` state the second; `ecommerce-dtc` states `discount_authority`. `zolts/catalog.py` carries the whole policy block into the overlay, so each declaration travels the full distance to a live programme and is then consulted by nobody — the gate reads one key out of the merged policy and it is none of these | **High**, and the exposure is reputational before it is technical. No document claims these are enforced, so nothing is contradicted; what misleads is the configuration itself. A partner's DPO or a buyer in diligence who opens `healthtech-lifesci.yaml` reads a prohibition on inferring Article 9 data, and an archetype meant to require human approval of copy does not require it. Auto-send is gated per play by `auto_send_requires.eval_score`, which a blueprint-wide declaration does not tighten | Read | `zolts/controls.py` gains a blueprint surface — a programme's `spec.policy` and a blueprint's are different schemas wearing one name, and the registry keeps them apart. `scripts/validate.py` names them per blueprint. Enforcement is a product decision about what an approval requirement *does*, not an implementation detail | `test_controls.py` asserts the regulated blueprints are the ones that trip it, by name, and that a blueprint control is never matched against a programme |
| D-65 | `spec.policy.overrides` described itself as *only overrides stricter than the tenant policy* and had no `additionalProperties: false`, so it accepted any key at all. `max_touches_per_week` is one word short of `max_touches_per_person_per_week`: it validated, nothing read it, and `gate.py` applied its default of **3** to a programme asking for **1** | **High**, and it runs the wrong way. Every other defect of this family is a control that does nothing; this one is a control that does the *opposite* of what was written — three times the contact pressure the customer specified, silently, on the axis a regulator cares about. A stricter override that does not take is worse than none, because the operator believes it did. `03-ecommerce-dtc` was shipping `discount_authority` in this block, accepted only because it was open | Read | The block is closed. An override the schema does not name is refused where the programme is validated rather than ignored where it is executed, which is the standard D-35 set for this block and it was applied to the four keys inside it and not to the block itself. `discount_authority` moves out; the blueprint is where commercial latitude is declared | `test_controls.py` asserts the misspelling is refused, that all four keys the runtime reads are still accepted — closing a block is only safe around the right set — and that no shipped programme declares an override nothing reads |
| D-68 | `scripts/seed_demo.py` seeds the example programme, which declares a send window of Mon-Fri 08:00-18:00 Europe/Madrid. `planner` moves each step to the next open rather than cancelling it — correct, and it means a demo run started outside those hours queues every touch for the following morning. The seeder's worker loop stops on the first tick that claims nothing, so the run reports **zero touches sent, zero replies, no measurement**, and `test_the_demo_seeds_a_tenant_that_enrols_measures_and_leaves_deals_alone` fails on `the demo sent nothing` | **High**, and it had been latent since the window was added. Fifty hours in a hundred and sixty-eight: **the suite was red about seventy per cent of the week on code that was never wrong** — every evening, every night and all weekend. It went unseen because everybody ran it inside European working hours; this run came in at 16:22 UTC, twenty-two minutes past the window's close, and CI had passed the same commit at 16:02. A check that fails on the clock teaches a team to press re-run, which is worth less than no check (D-35), and it is the third time this repository has found a test that fails on its environment rather than on the product (D-35, D-59) | Executed | The demo already rewrites the programme's tier capacities for exactly this class of reason and discloses it in its own output; the send window is removed the same way and disclosed in the same sentence. `window_for` returns nothing when a programme declares no window and `next_open` then leaves the moment alone, so this is the documented way to say *land now* rather than a special case | `test_demo_seed.py::test_the_demo_lands_its_sends_now_whatever_hour_it_is_run_at` asserts the shipped programme still declares a window — otherwise it proves nothing — that the sized one does not, and that a Saturday at 03:00 is not moved. Verified by restoring the window, which fails it and the original test together |
| D-69 | `runtime/reporting.py` counted `converted_by_type` — the per-outcome, per-arm breakdown of what a report's primary number is made of — over **any** time after enrolment, while the primary comparison and the opportunity arms printed beside it were both counted inside the metric window the programme declares. One query in that function had no `occurred_at < entered_at + window` clause and the two around it did | **Medium while it was a disclosure, high the moment it was divided into money.** A meeting booked two hundred days after enrolment on a ninety-day programme was counted in the breakdown and excluded from the lift, so the composition of a signed number did not compose it. Decision 45 then divides the meeting arm into spend: an unwindowed denominator against a windowed numerator would have reported a cost per meeting cheaper than the programme achieved, in a document a partner signs. D-51 was this defect one field to the left — every report counted the same three outcome types whatever the programme declared — and this is what it left behind | Read | Found while making the meeting arm load-bearing for decision 45; the fix is the clause the two queries around it already carried. The breakdown is now counted inside the declared window, like the number it composes | `test_cost_per_meeting.py`: two meetings on one programme, at day 2 and day 150 of a ninety-day window; only the first is counted. Verified by removing the clause again, which fails the test |
| D-70 | The incrementality report's cost table formatted credits with `:g`, which switches to exponential notation above six significant digits. A programme that billed 1,200,000 credits printed **`1.2e+06`** in the document a partner signs, and the total under it did the same | **Medium**, and it is the kind that reaches a customer rather than a test. The figure is money at a hundred credits to the euro; nobody reading a signed report parses a mantissa, and a reader who does not notice reads twelve thousand euros as something between one and a million. It survived because every programme in the suite and the demo billed under a hundred thousand credits — the threshold is six digits, and nothing had six | Read | Found while rendering a report whose spend was large enough to be realistic. A formatter that keeps what `:g` was there for, no trailing zeros on a whole number, and adds the thousands separator. Exponential is not reachable | `test_cost_per_meeting.py`: seven values across the threshold, and a rendered report whose cost table contains no `e+` |
| D-71 | `scripts/mutation_check.py` read a mutation's verdict from pytest's exit code. Eight of its eighteen guards live in `requires_db` tests, and a file whose tests all **skip** exits zero exactly like a file whose tests all pass — so run without a database the script broke eight guards, saw zero, and reported them as **survivors**: `10/18 guards bite`, in red, about code that was never wrong | **Medium**, and it is the fourth time this repository has found a check that fails on its environment rather than its subject (D-35, D-59, D-68). The verification gate is the one place where a false red is most expensive: the whole argument for it is that a guard erodes silently, and a contributor who sees eight failures on a clean checkout learns to disbelieve the output. The script already refused to report a mutation whose target string had moved — *not a pass and not a skip* — and the same reasoning had never been applied to the tests it ran | Read | Reading pytest's summary rather than its exit code was the first attempt and was not enough: `tests/test_metrics.py` holds five tests that need no database and five that do, so the file reported five passes beside five skips and the guard still read as a survivor. The script now establishes the premise before reporting any verdict — it collects each target under `-m db` and refuses to run at all when a guard's tests need Postgres and none is configured, naming them. A target whose tests all skip is separately `UNCHECKED` rather than a survivor. Ten guards that need no database still run on a laptop | `test_mutation_targets.py` asserts the refusal applies no mutation at all, that a guard needing no database still runs without one (or the refusal is a blanket one), that the probe reads the marker rather than a file name, and classifies a stubbed pytest result in each of its four shapes — all skipped, nothing collected, a real pass beside skips, and `-x` stopping at one failure |
| D-72 | `zolts.report.from_mapping` promises *the digest can be recomputed by anybody holding the JSON and nothing else*, and `canonical()` carried no version of its own shape. So a report frozen under one key set and rebuilt under the next recomputed the **new** shape: six keys the stored body never had, a different JSON, a different hash. A partner's letter and the digest their counterparty calculates disagree, and nothing says why | **High on the artefact this product is sold on, and latent only because nothing is deployed.** `docs/10` sells the frozen report on two checks: hash the JSON you hold, and rebuild it to re-derive the figures and the hash. The first never depended on this code and still passes; the second is the one that makes the document *auditable* rather than merely intact, and it broke the first time the form grew — in the change that added the cost per meeting. It gets worse with every key added, and the failure is silent in the direction that discredits us: the customer holding the letter looks wrong | Read | Found while adding the next canonical key for decision 46. `SCHEMA_VERSION` names the current shape and `KEYS_ADDED_IN` keeps every earlier one. `canonical()` emits the shape the report's own version declares, `from_mapping` reads that version back — a body with no version key is version 1 — and the renderer omits a section whose figures the version never carried, so a re-render is the document that was signed. ADR-043 records that every change to the key set is a new version with its keys named | `test_report_schema_version.py` pins a body **and** its digest captured from the code at `46ff70d`, before the form was versioned, and verifies it against today's. The fixture is the artefact rather than a recomputation, because a test that recomputes both sides proves only that the code agrees with itself. Verified by restoring the unversioned `canonical()`, which fails three of the eight |
| D-73 | **Twenty-four** tests assert what the **restricted** role cannot do — eighteen of them tenant scoping, two the write-once documents a partner signs against, two preflight's own readiness. `tests/conftest.py` falls back to the owner URL when `ZOLTS_TEST_APP_DATABASE_URL` is unset, which is right for a test that needs *a* database and a lie for one that is about a role: the pool then connects as the owner, who bypasses every grant, and all twenty-four fail | **High**, and the fourth instance of the shape in a week. A contributor cloning the repository and exporting the one obvious URL gets twenty-four red tests, eighteen of them the isolation properties `docs/11` calls the ones a breach of would end the company — on code that is correct. **Nine was the first count and it was wrong**: nine is what a hand probe over three files could see, and the other fifteen sat in eleven files nobody had opened. That teaches a team to press re-run, and a team that presses re-run has no suite (D-35, D-59, D-68, D-71). The RLS policies are `force row level security` so the *policy* tests would pass either way; it is the grant-based ones that cannot be observed as the owner | Read | Found while writing the same assertion for a new table. `requires_app_role` skips with a reason that names the variable, and the `app_role_is_restricted` fixture asserts the connection really is a different role from the owner before any verdict — a URL pointing at the owner by mistake satisfies a skipif and proves nothing. CI refuses a green run that skipped them, the same way it already refuses one that skipped Postgres entirely | `test_period_spend.py` holds both halves: that the skip exists with its reason and that CI greps for it, and that the fixture checks the connection rather than the variable. Verified by running the whole suite with the app URL unset — twenty-four failures before, twenty-four skips after — and thereafter by `scripts/ambient_check.py`, whose `no-app-role` condition is this defect made repeatable |
| D-74 | `test_warmup_is_a_date_and_not_a_counter` seeded a mailbox at `date.today() - 13 days` and asserted the runtime read it as warm-up day 14. `runtime/fleet.py` counts from the **UTC** date, deliberately and correctly; `date.today()` is the machine's *local* date. East of UTC after local midnight the two differ by a day, so the test asserted 14 against a correct 13 | **Medium**, and it is the sixth of the shape (D-35, D-59, D-68, D-71, D-73). The product was right in every timezone — a mailbox is the same age everywhere, which is the whole point of counting warm-up in UTC, because the mailbox's provider is counting in one timezone and it is not the reader's. The test was the thing that moved. It would have been red for twelve hours a day for anybody in New Zealand and never once in Europe | Executed | The test establishes the UTC day itself, asserts the row holds it, and only then takes the verdict. A second test reads the same fleet at two instants that share a UTC date and differ by local date, and requires the same warm-up day | `test_fleet.py`, run under `TZ=UTC`, `Pacific/Auckland` and `Pacific/Honolulu`. Verified by restoring `date.today()`, which fails east of UTC and passes at home — the two-sided check the original never had |
| D-75 | `docs/23` lists *CI fails a run that skipped the isolation tests* against `test_ci_never_reports_green_having_skipped_these`, and that test asserted only that `ZOLTS_TEST_DATABASE_URL` was set. Twenty-four of those tests need a **separate app role** to observe anything (D-73) and skip without one, so a CI run with the owner URL alone would have passed the guard, skipped every isolation test and reported green | **High**, because of what it is a guard for. The register names it as the thing standing between a green build and an unverified isolation property, and it covered half the condition. The failure is silent in the direction that reassures: nothing is red, the document says the property is guarded, and the twenty-four tests that would have observed it never ran | Read | Found while checking whether any document quoted D-73's old count of nine. The guard now asserts a distinct app role as well, with the reason in the message rather than in a commit | `test_runtime_isolation.py`, verified both ways: with the app URL set and `CI=true` it passes, and with the variable removed it fails and names it |
| D-76 | All four shipped archetypes declared `experiment.guardrail_metrics` and nothing evaluated them; `zolts/controls.py` carried the field as the sharpest of eleven unenforced controls, because a programme that wins on its primary metric while damaging a guardrail reports an unqualified win. The wiring was the smaller half. **None** of the six names they used existed in `zolts.metrics.METRICS`, and four of them could never: an unsubscribe rate, a spam-complaint rate, a complaint rate and a bounce rate are recorded on the **touch** (`unsubscribed_at`, `complained_at`), and the holdout is never touched — `planner` stops on `variant == "control"` as product invariant 4. Their control arm is structurally zero, so the verdict is permanently *not resolvable* | **Medium** — a signed document with a guardrail section that would have read *not resolvable* for ever, indistinguishable from a programme still gathering data, in the section a buyer reads to check the win was clean | Read | A guardrail must resolve in the registry, be a rate rather than a value metric, and differ from the primary metric; refused at admission with the reason and a pointer to ADR-020's deliverability rules, which do enforce those limits per mailbox and per domain. What survives is measured against the same holdout and qualifies the verdict paragraph (ADR-047, decision 47). Three archetypes now declare none and say why in the file | Forty-five tests, including four mutants killed by hand: compose measuring nothing, a guardrail that is never degraded, admission not checking, and a touch-derived name let through. `test_registers.py` fails the day the schema grows a control `zolts/controls.py` does not name |
| D-78 | The e-commerce archetype's *converted* exit rule tested `outcome.type == 'purchase'`. The runtime records a completed sale as `won` (`inbound.CONVERSION`, from the provider's `deal.won`), which is also what that programme's own `net_revenue_28d` counts. So the one rule that ends a winback sequence when the customer buys named a type nothing writes | **Medium** — a customer who repurchases keeps receiving the winback sequence, on the archetype whose whole subject is not annoying existing customers | CI | The rule tests `won` | Found by the lint added for D-77, on its first run, rather than by anyone reading the file |
| D-79 | `zolts/waterfall.py` has documented the accuracy exclusion since the reference core — *a provider whose measured accuracy is below the SLA is excluded outright, because a hit from it would deliver a value the program promised not to send* — and `runtime/enrichment.py` called `optimise(providers, cohort)`, leaving the floor at its default of zero. `eligible = [p for p in providers if p.accuracy >= 0.0 ...]` excludes nobody. The name `accuracy_sla` appears nowhere under `runtime/` at all. The flagship archetype ships `accuracy_sla: 0.95` with the inline comment *the router picks a waterfall that meets this*, `docs/04` sells it in the block table and `docs/07` states it as the constraint the optimiser solves under | **High** — a customer who buys high-confidence contact data is sent whatever the cheapest provider returned, and the provenance record they can read proves the value came from a provider below the floor they declared. It is a quality promise in the contract sense, on the axis the enrichment product is sold on | Read | The floor travels from the programme block through `enrich_step` to the optimiser on the same path as the cost cap; a field no provider meets is reported as an excluded floor rather than as a miss (ADR-049, decision 49) | Four tests and four mutants killed by hand, including the end-to-end one — which first passed because the fixture's contact already had the field being bought, so nothing was purchased either way |
| D-81 | `spec.route.tiers[].capacity_per_week` was read by nothing: the name appears nowhere under `runtime/`, so routing admitted without counting. `docs/04`'s block table states the contract as fact — *thresholds to tier plus per-tier capacity; **human capacity is a finite resource and is modelled as one*** — and the flagship programme caps t1 at 25 with the comment *the team's real human capacity*. It hid behind a real capacity gate: `runtime/fleet.py` and `worker.py` do enforce capacity per mailbox and per domain (ADR-020), so an operator watching deferrals work concludes the programme's own block is what works — the same confusion the tenant credit ceiling created for `spec.budget` in D-63 | **High** — t1 of a 1:1 motion is the human-reviewed tier (`zolts/dsl.py` refuses a t1 play that auto-sends), so over-admitting fills a review queue past what anybody can work: proposals age out, or are approved unread, which is the failure the propose-then-dispose invariant exists to prevent. It over-enrols rather than over-sends, so no domain is burned — the waste is the enrichment and generation credits already spent on accounts nobody will work | Read | Counted at the routing choke point; a full tier falls through to the next one the account also qualifies for, and the audit log distinguishes a ceiling from a scoring miss (ADR-050) | Eighteen tests and six mutants killed by hand. The holdout test first passed with its guard removed, because assignment is a hash and both accounts that took the tier happened to be treatment; it now writes one enrolment per arm |
| D-67 | `docs/24` listed Signals under *delivered and verified against real infrastructure*: nine definitions and a runtime that goes and looks. The engine is real — refresh clocks, decay, dedupe, per-account-day billing and the latency split all execute and are tested. **No source ships.** Every definition names a connector — `jobs_feed`, `press_feed`, `product_events`, `commerce_events`, `places_feed`, `ads_intel` — that no shipped code registers; `install_default_connectors` registers CRM read sources and no signal source, and the only implementation of the protocol is a fake that a test registers for itself | **Medium**, and entirely a documentation defect: the code was never wrong. `watch.once` resolves the source *before* asking whether anything is due, with a comment saying why — a tenant with no accounts and a tenant with no sources both look like a quiet pass otherwise, and only one is a deployment somebody has to fix — so it reports `no signal source '<connector>'` per definition rather than crashing. **The runtime's error message was more honest than the backlog.** The cost is to a reader: the whole point of `docs/24` is that a buyer can tell built from planned, and the largest subsystem in the table was on the wrong side of that line | Read | `docs/24` says the engine ships without a source and names what the first partner integration unlocks; `docs/20` says `zolts watch` reports the absence on the first pass. Registering real sources is partner integration work, not a correction | `test_signal_sources.py` holds the documents to the code: if a real source is ever registered, the test fails until `docs/24` is corrected in the same change. Verified by asserting the opposite claim, which fails |
| D-66 | `zolts connect --secret-stdin` was `action="store_true"` with `default=True`, so it could never be false, and no code read `args.secret_stdin`. The secret is read from stdin unconditionally | **Low**, and worth a row because of what it implies rather than what it does. A flag naming a mode implies a second mode: an operator following `docs/26` under pressure may look for `--secret-file`, and there is none by design. The `default=True` is the tell — a `store_true` that is on whether or not you ask. Had anyone later added a real alternative, this flag would have silently won | Read | Kept and made honest rather than removed: `docs/20`, `docs/25` and `docs/26` all put it in the command an operator copies, and rejecting it would break three documented invocations to delete a word. Its help now says it is accepted and ignored, and why stdin is the only path | `test_controls.py` fails if anything starts reading the flag while its help still says it selects nothing |
| D-63 | `spec.budget` promises a programme-level spending regime in five fields. One is honoured and narrower than it reads — `monthly_credits` bounds **copy generation** (`generate.py`), one of the eight priced actions. `on_exceed` is read by a single validation rule and by no runtime code, so `pause_and_alert`, `throttle` and `continue_and_alert` are the same programme — and it was a console dial an operator could set (ADR-029). `max_cost_per_account` in this block is enforced by nothing; the cap of that name that *is* enforced is `spec.enrich.account.max_cost_per_account`, a different field. `max_cost_per_person` and `max_cost_per_meeting` are read by nothing at all. `docs/07` named per-programme, per-tenant and daily ceilings plus `on_exceed` as the control for spend leakage; only the per-tenant ceiling exists | **High.** It is money, it is customer-declared, and it fails in the flattering direction. The tenant ceiling *does* fire and defers actions with `budget: …`, so an operator watching budget enforcement work has every reason to conclude the programme's own block is what works. A customer running two programmes cannot cap one of them, and a partner's success-criteria letter that quotes a per-meeting ceiling quotes a number nothing holds. Six other declared controls across routing, plays, enrichment and experiments are unenforced the same way — every shipped programme declares five or six | Read | `zolts/controls.py` names every declared control, where it is enforced or what its absence costs. `budget.on_exceed` is out of the tunable set, because a dial that reports success and changes nothing is worse than an absent one. `scripts/validate.py` prints a programme's unenforced controls when it validates it, and `docs/07` is corrected to claim only the ceiling that exists. Enforcement itself is decisions 44 and 45 | `test_controls.py`: every registry path resolves against the schema, every unenforced entry states its cost, and **no tunable may appear in the unenforced half** — verified by restoring `on_exceed` to the tunable set, which fails it |
| D-62 | `repo.entities.suppress` existed from the first migration and had no operator surface. Every caller was the runtime acting on a provider's event, so the one moment a person most needs it — the inbound handler has broken, the runbook says apply the unsubscribes first, and the automation that would have done it is the thing that is down — had no command at all | **High**, and it is a compliance exposure rather than an inconvenience: honouring an unsubscribe is not optional and had no manual path. Found while writing the D-61 runbook procedure, which referred to `runtime.cli suppress` as though it existed — the register's own dominant shape, produced by the author of this row and caught before it shipped | Read | `runtime.cli suppress`, with scope, value and a reason recorded as `source = operator` so the audit trail says a human decided. Idempotent, and it reads before writing so a repeat confirms rather than silently rewriting the reason on file | `test_suppress_command.py` asserts the send actually stops, that the address matches however it was capitalised, that a domain suppression reaches an address behind it, and that a second run leaves the recorded reason alone |
| D-59 | The preflight check for encryption in transit has three branches — TLS negotiated, no TLS but the database is local, no TLS and it is not. The two tests named for the no-TLS branches never established that their connection was without TLS; they inherited whatever the ambient server did. CI's `postgres:16` container ships `ssl = off` and a Debian-packaged Postgres ships `ssl = on`, so the same two tests exercised the branches they name on one machine and the TLS branch on the other | **Medium**, and the cost is to the team rather than to a customer: the product is correct in both configurations, and the suite is red on a contributor's laptop and green on CI. A test that fails on correct code teaches people to press re-run, which is worth less than no test at all (D-35). The fatal branch — a managed database reached unencrypted over the public internet — was the one going unasserted wherever a developer's Postgres had TLS on | Executed | The tests open their own connection with `sslmode=disable` and **assert the premise before the verdict**: if that connection negotiates TLS anyway, the test fails saying so rather than quietly proving a different branch | `test_preflight.py`; verified by running the suite against the same cluster with `ssl = on` and `ssl = off`, red then green on the first and green on both after |
| D-58 | The console rounds a frozen report's lift to two decimals, the unit the rest of the measurement panel is in. A test asserted that figure — as `shown["liftPp"] == round(frozen[...]["lift"] * 100, 2)`, the code's own expression re-run, on a fixture whose lift was exactly 15.00pp. Two decimals and three agree on 15.00, so the assertion was both self-referential and fed data that could not tell them apart | **Low** in consequence — a third decimal on a CFO's screen, in a panel whose every other figure is in two — and **high** in what it says about the guard. An assertion that re-runs the code it checks certifies whatever the code does; this one would have survived any rounding change at all. `schemas.py:136`, fixed in the same commit, is the same class: a baseline date's ten-character ceiling loosened to eleven, on the window a partner signs a letter against, and no input anywhere was eleven characters long | Mutated | The expectation is a literal, on a lift of 23.333…pp, and the test asserts **first** that the fixture still discriminates — so counts changed later fail loudly instead of going quiet. The date ceiling is asserted at exactly eleven characters, the only length that separates the two | `test_survivors.py`; re-applying each mutation fails exactly its own test and no other |
| D-57 | `test_the_budget_is_under_the_cron_interval` derived the cron interval from the minute field alone — `60 if "*" else 60 * int(field)`. It had only ever been fed `* * * * *`, so it had only ever computed 60. Moving the shipped schedule to a daily one (decision 43) made it compute `60 * int("0")` — zero seconds — and fail a schedule that is perfectly safe | **Medium**, and the failure is the harmless half. A daily schedule of `30 3 * * *` would have computed 1800 seconds and **passed**, having measured nothing about the real twenty-four-hour gap — a guard that is green because its arithmetic is wrong in the flattering direction. The budget it protects is what stops two ticks overlapping on the same leases | Executed | The reader covers the subset the repository ships — `*`, `*/N`, and a fixed minute or hour — and **refuses** anything else rather than returning a plausible number. A weekly or monthly expression raises rather than being silently read as daily | `test_serverless.py::test_the_interval_reader_refuses_what_it_cannot_read` asserts six readable expressions and four it must refuse |

### Product surface

| ID | Defect | Blast radius | Found | Fix | Guard |
|---|---|---|---|---|---|
| D-13 | Five rail entries led nowhere | **High** — a nav that promises what it cannot do | Looked | Every entry has a view | ADR-023 |
| D-14 | Five console views wore the Programs header over columns sized for a different table, so values wrapped and rows grew into each other | **Medium** | Looked | One `--cols` shared by header and rows | A test asserts each view labels exactly the columns it has |
| D-15 | At 390px the rail, the nav and the detail panel were `display:none` and 18 elements were clipped: one screen with half a table on it | **High** — the product was unusable on a phone | Looked | Rail as a scrolling strip; rows stack into label/value pairs | `browser_console.py` fails on an unreachable rail, a clipped cell or horizontal overflow at 390px |
| D-16 | Text columns were fixed pixel widths. A 1920px display gave ~500px of spare width to empty gutter while `local-services-multisite` was cut off inside a 152px column | **Medium** | Looked | `minmax(px, fr)` for variable-length text; `w:` declares prose | ADR-028; a wide-screen check at 1920px |
| D-19 | ADR-002 said "the UI generates DSL". Nothing did. The button was labelled *Open in editor* and did nothing | **High** — a documented architecture decision with no implementation, read by diligence | Read | Eleven money-and-risk dials emitting a whole document | ADR-029; the browser publishes and the assertion is in SQL |

### Guards that did not guard

The category that only appears once you go looking for it.

| ID | Defect | Blast radius | Found | Fix | Guard |
|---|---|---|---|---|---|
| D-17 | `browser_console.py` detected the wide-screen clipping, printed it, and returned 0 | **High** — worse than no check: a defect reported and a build gone green | Mutated | Returns 1 | A screenshot check that finds a defect must exit non-zero; asserted by a test |
| D-23 | Nothing stopped an agent importing a connector and sending directly. It happened not to — a fact about four files, not a property of the system | **Critical** if it had ever been relied on — an email nobody approved | Mutated | No module under `runtime/agents/` may import the machinery that acts on a prospect | An AST bar: no module under `runtime/agents/` may import a connector |
| D-24 | CI built the image, booted it and curled it — all on the image's default CMD. `fly.toml`'s two process commands were run by nothing, and the worker is the product | **High** — a deploy with an API that answers and a worker that crashloops; the symptom is a queue that grows with no error | Read | A test reads the process table; CI runs both commands in the container | CI runs each `fly.toml` process command inside the built image |
| D-25 | `scripts/restore.sh` had never been executed. Two tests asserted the *reasoning* behind its first step | **Critical** — a restore happens after a customer's data is gone | Read | The script is run end to end against a real dump | The script is executed end to end against a real dump in CI |
| D-26 | The first version of that restore test restored into a new database **on the same cluster**, where the role already exists, so the condition the script guards against never arose. Deleting the whole role-creation step left every test passing | **High** — a test that certified a backup nobody had tested | Mutated | The dump is rewritten to grant to a role the cluster does not have | The dump grants to a role the cluster does not have |
| D-27 | The step-3 test faked a stale dump by deleting a bookkeeping row, producing a state that cannot occur, and its failure was first read as a defect in migration 018 | **Low** — a wrong diagnosis, corrected before it became a wrong fix | Executed | A real predecessor is built: every migration but the last, applied and recorded | A real predecessor: every migration but the last, applied and recorded |
| D-18 | `site/` was stale against `design/console.html` | **Low** | CI | The build is regenerated and CI fails on drift | CI rebuilds `site/` and fails on drift |
| D-32 | `scripts/smoke_runtime.py` returned `0` unconditionally. Its docstring said it proved "the whole loop is connected"; it printed a report and exited zero with nothing enrolled, nothing sent and no decision recorded. It runs in CI as the one end-to-end check | **Critical** — the single check standing between a disconnected product and a green build could not fail | Executed | Each stage must have happened, and a missing one names itself | Verified by breaking the fixture: the account no longer matches the audience and the run exits 1 naming four dead stages |
| D-33 | `spec.enrich` was never read by the runtime. Enrichment happened only when an operator typed a CLI command, and the fields the shipped programs requested — `work_email`, `linkedin_urn`, `tech_stack` — were not the fields the price list carries, which are `email`, `phone` and `firmographics`. It could not have worked if it had been called | **High** — the waterfall is priced, tested and measured, and no program triggered it. For a Clay-class product this is the core loop | Executed | The worker buys the declared fields before the first step; decision 33 cut the programs to the three fields that have a price, and a field without one is refused at publish | A test buys a declared field end to end, and a second asserts the buyable set is exactly what `zolts/billing.py` prices |
| D-31 | One subprocess call in the new restore tests inherited the environment without the `ZOLTS_SECRET_KEY` fallback its siblings carry. `Settings.from_env` builds the whole configuration before dispatching, so even `migrate` refuses without a key it never uses. It passed locally, where the variable is exported, and failed in CI, where it is not | **Low** — but the class is not: a test whose requirements are undeclared certifies the machine it ran on, not the code | CI | One helper declares what every subprocess in the file needs; the failure was reproduced with `env -u ZOLTS_SECRET_KEY` before and after | One helper declares what every subprocess in the file needs |
| D-34 | Four admission checks — a declared holdout, an audience the runtime can evaluate, an enrichment field that has a price, a policy override that can be read as stricter — ran in `POST /v1/programs` and nowhere else. The other four paths to a stored program (`zolts bootstrap`, self-service signup, the demo seed, the smoke run) ran none of them | **High** — signup publishes into a live tenant with no operator watching. A program with no holdout, an audience by `segment_ref` that nothing resolves and an unpriced enrichment field was accepted through it and activated; the API refuses the same document three times | Executed | Admission moved into `runtime.repo.programs.publish`, the one function all five callers already go through | Three tests publish the way signup publishes and assert the refusal; a fourth parses `publish` and fails if the check leaves it or moves after the insert |
| D-35 | `scripts/smoke_runtime.py` failed roughly one run in ten, and the failure was correct product behaviour: the account it enrols is assigned to treatment or control by a hash of its id, and the shipped program holds back 10%. A control enrolment produces no action, so four stages reported dead for the one reason that is not a defect | **High** — a CI check that fails one run in ten teaches the team to press re-run, which is worth less than no check at all. It would have arrived as flakiness in somebody else's pull request | Executed | The holdout is not turned off — it is the product's central invariant and the program is still published verbatim. The script picks an account the real assignment function puts in treatment, and says so if twenty in a row land in control | Five consecutive green runs, against two failures in three before the fix |
| D-36 | `GET /health/liveness` answered **200** with `draining: false` in the body. `docs/20` says "point a monitor at it" and what a monitor reads is the status code, so an uptime check wired exactly as instructed reported a healthy deployment while the outbox was stalled, a domain was burned or every worker was dead. `scripts/smoke_deployed.py` already accepted `503` as a valid answer — for a code nothing sent | **High** — the one signal that says the product has stopped working, published on a path designed to be watched, unable to fail where it is read | Read | 503 when any signal fails, including the database-is-gone path. Fly probes `/health`, not this, so a stalled outbox does not restart the API | Two tests assert the status code; verified by reverting the line and watching the stalled-outbox test fail |
| D-37 | A trigger predicate compared a payload field it could not compare — a number sent as a string, a null where a figure goes — and raised `TypeError` out of `POST /v1/signals` as a **500**. The code's own comment two lines above says signal payloads are external data and a partial one is *a non-match, not an outage*; it was true of an absent field and untrue of a present, unusable one. Quoting numbers is what a great many JSON producers do and what every form-encoded webhook does | **High** — the front door for data this runtime does not control, answering the one status code that makes a customer's integration retry for ever and tells nobody why | Executed | A predicate that cannot be answered is a non-match, reported to the sender in the response and to the operator as `signal.unanswerable` in the audit log | Nine tests, including the boundary: an *absent* field stays silent by design and an unusable one is named. In `mutation_check.py` |
| D-38 | Three things drew on top of text at 390px, and every layout check passed through all of them because nothing was clipped and nothing overflowed. **(a)** `.row.sub .cell.k{padding-left:0}` put a sub-row's text under the absolutely-positioned status dot, so a mailbox read `e@outbound.example` — the dot sitting on the first character of the address an operator uses to tell two mailboxes apart. **(b)** A prose cell's `-webkit-box` line clamp beat the phone's `display:flex`, so its label lost its 96px column and rendered `reasongoogle`. **(c)** `.draft` matched both `<pre class="draft">` and the status marker `<span class="st draft">`, padding a 9px dot into a 28x24 block struck through every program name | **Medium** — the console is the surface a buyer opens, and on a phone it showed truncated mailbox addresses and struck-through program names | Looked | Each cause fixed at its own rule; the copy-block rule is `pre.draft` now, because a status value and a component sharing a word is a collision waiting for the one place they are both visible | `browser_console.py` measures where the *text* starts, not the box, and fails when a marker covers any of it. Verified by putting the class collision back: exit 1, naming the eight program names it covers |
| D-39 | `outbox draining` fails only when work is due and unclaimed for thirty minutes, and while nothing is due it cannot fail. A worker that died on a quiet Friday — or, on the host the founder chose, a cron that never fired — was reported healthy until Monday's first action was due, and half an hour more. `docs/20` says the endpoint answers whether the deployment is doing its job; it answered whether the outbox had stalled, which is a subset | **High** — the one signal that says the product has stopped working, blind for exactly as long as the product is quiet, which is most of the time | Read | Every tick writes a heartbeat; liveness fails when the last one is older than five minutes or there has never been one. A fresh deployment is not draining until something has ticked | Four tests, including the stale heartbeat and the deployment nothing has ticked; in `mutation_check.py` |
| D-40 | The root `requirements.txt` — the only file Vercel's Python builder reads — said `-r runtime/requirements.txt`, so the two lists could not drift. The builder's parser refuses an include line (`PYTHON_REQUIREMENTS_PARSE_ERROR: Error parsing included file`), so the function did not build. The test written the same hour to guard the file asserted that the include was present: it passed, and certified the failure | **High** — the first production release would have failed on the platform after every check in CI was green; CI installs the runtime's list directly and is structurally unable to notice | Executed | The root file is a literal copy of the runtime's list, and the test asserts the two lists are equal and that no include line exists | `test_the_root_requirements_are_the_runtimes`; found by a probe deployment to the real project, which is the only thing that runs the platform's parser |
| D-41 | `docs/17` calls capturing a tenant's baseline *the irreversible phase-1 requirement*, `docs/13` makes it an exit criterion (100% of tenants within seven days) and `docs/14` a KPI. Nothing stored one: the word appears in the runtime only as the control arm's rate in an MDE calculation. An onboarding that followed the documents had nowhere to put the number they insist on | **High** — it is the one thing the documents say cannot be reconstructed later, and the month-9 expansion (`docs/17`) rests on it | Read | `tenant_baseline`, written once, with the derived figures stored and a digest the pilot letter quotes; activation without one is noted in the response and the audit log (ADR-042, decision 35) | Fifteen tests, including the second capture that is refused and the activation note that disappears once the row exists; `docs/26` is checked command by command |
| D-42 | ADR-011 said *a tenant can route through their own gateway*, and `docs/23` repeated it as the mitigation for the model provider seeing prospect text. `ModelClient` reads one `ZOLTS_MODEL_BASE_URL` from the environment: the endpoint is per deployment, and no tenant has ever had a place to store one | **Medium** — a security document promising a customer control that does not exist, to the one reader who asks for it in diligence | Read | Both documents corrected to what ships; `docs/11` states what each agent sends and never sends, and that the layer is off by default; per-tenant routing is decision 36 | `test_model_provider_disclosure.py` reads the variable's name out of the document and the code and fails if either drifts, fails if agents come on by default, and fails the day either document promises per-tenant routing again |
| D-43 | Decision 35 says an activation without a baseline is *noted where an operator looks*. The API put the note in the response and the console's activation handler answered a 200 with `window.location.reload()`, which discarded the body: the one screen an operator looks at showed a live program and no note | **Medium** — the note exists to prevent a tenant reaching month three with no baseline, and it was delivered to a JSON body nobody reads | Read | The handler keeps the note across the reload and the detail panel shows it on the program it was about, until the operator moves to another one | `browser_console.py` activates a draft on a tenant with no baseline and fails unless the note is on the screen |
| D-44 | `scripts/smoke_runtime.py` proved the loop only Monday to Friday, 08:00-18:00 in Madrid. The flagship program declares that sending window and the planner moves a send into it rather than dropping it, so outside the window — every evening, every weekend — the step the tick had just planned was due the next morning, nothing was claimed, and the script reported four dead stages for correct behaviour. Every merge that ever ran it landed inside the window | **High** — the one end-to-end check in CI, red on any push after six in the evening or on a Saturday, which teaches the team to press re-run (D-35 again, by the clock instead of by the coin) | Executed | The window stays. A step deferred by the window alone is pulled forward in the smoke tenant, the report says so, and the stages are proven at 21:53 Madrid | Run outside the window with `deferred_by_window` non-empty and every stage green; the window itself is `zolts/schedule.py`'s own tests |
| D-45 | `docs/02`'s stack table chose TypeScript, Temporal and ClickHouse, and its *Durable runtime* section named Temporal as the engine. The runtime is Python, the engine is a Postgres outbox with leases (ADR-007, in the same document) and nothing reads ClickHouse. `docs/19` said so in one sentence; the second document a reader opens said the opposite in a table, and `docs/01`, `docs/13` and `docs/16` repeated the plan as fact | **Medium** — the same exposure as D-21, four documents wide: a security or technical review reads the stack table and asks to see the Temporal cluster | Read | The table keeps the plan and gains a *What ships* column; the section, the moat row, the roadmap row and the role description name the outbox and cite ADR-007 | `test_the_stack_table_names_what_ships`: the runtime row names the outbox, the language row names Python, and no `.ts` file exists under `runtime/` or `zolts/` |
| D-21 | ADR-003 claimed zero-copy over the customer's warehouse in the present tense. The word `warehouse` appears nowhere in `runtime/` | **High** — a security review asks to see the connector and there is none | Read | The document is corrected to what ships (decision 29) | A test fails in both directions: while nothing reads a warehouse the document must say so, and the day something does the document is stale |
| D-77 | `runtime/engine/planner.apply_exits` is called by one test and by no production code — `spec.get("exit")` is read in that function alone. The whole `exit:` block of every shipped archetype was declarative content the runtime never evaluated, so an enrollment never exited on `outcome.type in (...)` (*converted*) nor on `days_in_program > 45` (*exhausted*). It is invisible because the two exits that **do** fire cover the ends: `sequence_complete` when the steps run out, and `opted_out` from `inbound._opt_out`. And `zolts/dsl.py` linted for an exit rule setting `suppress`, reporting *opt-outs would not be recorded* — the only rule satisfying it in all four programmes tested `outcome.type == 'unsubscribe'`, and nothing writes an outcome of that type: triage routes that verdict to the suppression path before an outcome exists. `suppress` itself was read by nothing | **High** — a person who books a meeting keeps receiving the rest of the sequence, which is the failure a prospect notices and names; and a lint that certified opt-outs were recorded was answered by a rule that could never fire | Read | Applied at two call sites: the tick, and the moment an outcome is recorded — the second because `plan_next` queues the next step with its wait already applied, so a tick-time exit runs after that email has gone. `suppress` writes through the same `entities.suppress` an opt-out uses, and names itself when there is no address to write. The lint now checks what a rule can be wrong about (ADR-048, decision 48) | Twenty-one tests and five mutants killed by hand. The first version of the redelivery test passed with its guard removed — `store` deduplicates the webhook before `apply` is reached — so it re-applies one stored event instead. The new lint found a second dead rule on its first run: D-78 |
| D-80 | `tests/test_controls.py::test_the_console_never_offers_a_dial_the_runtime_does_not_honour` is the guard written after D-63 to make that defect *a class rather than an incident*. It computes `{p for p, _ in dsl.TUNABLE} & {c.path for c in controls.NOT_HONOURED}`, so a dial escapes it entirely by not being a registered control — and five of the ten were unregistered. Two of those five were read by nothing: `spec.enrich.*.accuracy_sla` (D-79) and `spec.route.strategy`, whose only occurrence in the whole shipped codebase was the line in `zolts/dsl.py` offering it as a console dial labelled *who receives the enrolled account*, so `score_desc`, `territory_round_robin` and `owner_of_record` are the same programme | **Medium** — the same cost as D-63 (a dial an operator sets, that saves, versions and changes nothing) and worse in kind: the guard built to prevent exactly that could not see it | Read | The guard asks the code rather than the registry: a dial's key is a name in a document, so code reading one carries it as a string literal, and a dial no literal names fails. `spec.route.strategy` leaves the dial list on the terms `budget.on_exceed` left it | The check is asserted in both directions — a leaf nothing mentions comes back empty, and the two dials whose key is passed as a variable (`_cap(block, "max_cost_per_account")`) do not, because a check that flagged those would fail on correct code |
| D-82 | `spec.route.tiers[].capacity_per_week_per_rep` counts against a representative, and **there is no representative anywhere in the data model**. No `user` or `seat` table exists; identity is a tenant and an API key, and the review queue records `approved_by` as `key:<uuid>`, a credential rather than a person. Neither `account` nor `enrollment` carries an owner column — only `opportunity` does, filled from `hubspot_owner_id` long after routing, so `hubspot.py` reads an owner the account layer drops. `spec.route.strategy`'s `owner_of_record` and `territory_round_robin` route to the same absent person | **Medium** — two schema fields and a console dial describing a capability that cannot exist until something models a rep. Unlike every other unenforced control here, no amount of code against the current schema would honour it | Read | Open: decision 50 registers the choice — record the owner the CRM already gives us, or model seats — with a default. Both fields stay registered as unenforceable, and a test fails the day an owner column or an identity table appears, so the decision is ready to close rather than forgotten | Not yet guarded beyond that: found while establishing what `capacity_per_week_per_rep` could count against (D-81) |

## What this register is not

It is not a list of production incidents. There have been none, because there is no production. The first entry in this table that involves a customer will be of a different kind, and the honest expectation is that it will be found the same way: by running something, not by reading it.

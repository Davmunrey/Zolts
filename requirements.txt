# What the Vercel function installs. Vercel reads this file at the repository
# root and nowhere else, and its parser refuses an include line: a root file
# that said `-r runtime/requirements.txt` failed the build with
# PYTHON_REQUIREMENTS_PARSE_ERROR (D-40). So this is a copy of
# runtime/requirements.txt, and a test fails the moment the two differ.
# Production runtime dependencies. The reference core in zolts/ needs none of
# these: it is pure logic and stays that way.
psycopg[binary,pool]>=3.2
cryptography>=42.0
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pyyaml>=6.0
jsonschema>=4.20

# The agent layer. Optional at run time: a deployment with ZOLTS_AGENTS unset
# runs every non-agent program without it.
anthropic>=1.4
# Cost governance is Trazum's `spend_guard`, over MCP:  npm i -g @trazum/mcp
# It is a Node package rather than a Python one, so it is not pinned here.
